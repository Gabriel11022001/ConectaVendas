import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

  imgRecuperarSenha: {
    width: "80%",
    height: 300,
    resizeMode: "cover"
  },
  containerImageRecuperarSenha: {
    width: "100%",
    alignItems: "center",
    justifyContent: "center",
    marginVertical: 30
  }

});

export { styles };

